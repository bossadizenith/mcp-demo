{
  "mcpServers": {
    "incido": {
      "type": "stdio",
      "command": "bun",
      "args": [
        "run",
        "--cwd",
        "${workspaceFolder}/packages/mcp",
        "src/index.ts"
      ],
      "env": {
        "API_URL": "http://localhost:4000"
      }
    }
  }
}
