{
  "mcpServers": {
    "incido": {
      "command": "bun",
      "args": ["run", "--cwd", "packages/mcp", "src/index.ts"],
      "env": {
        "API_URL": "http://localhost:4000"
      }
    }
  }
}
